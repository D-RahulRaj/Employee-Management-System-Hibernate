# Employee Management System – Hibernate

A standalone project built using **Hibernate ORM** to manage employee information.  
Implements full CRUD operations using Hibernate’s core features.

## ⭐ Features
- Add, update, delete, and view employees
- Entity mapping using annotations
- HQL/JPQL queries
- Proper session & transaction management
- Lazy loading for performance improvement

## 🛠 Tech Stack
- Java  
- Hibernate ORM  
- MySQL/Oracle  
- Hibernate Configuration XML / Annotation-based config  

## 📁 Modules
- `Entity` – Employee model  
- `DAO` – CRUD operations  
- `Util` – Hibernate session factory setup  

## 🚀 How to Run
1. Add Hibernate + MySQL connector JAR
2. Configure `hibernate.cfg.xml`
3. Run the `main` file to test CRUD operations
